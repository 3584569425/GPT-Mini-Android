# AI Mini 应用介绍与使用帮助

## 应用简介

AI Mini 是基于 Codex Mini WebUI 使用场景开发的 Android 手机 App。它保留了 WebUI 的核心体验，并使用 Mozilla GeckoView 浏览器内核，针对液态玻璃流畅度、键盘适配、下载管理、通知提醒、多设备链接切换等使用问题进行了优化。

本 App 适合需要在 Android 手机上稳定访问 Codex Mini WebUI 的用户使用。用户可以通过手动输入链接、扫码连接、继续上次连接、已保存连接等方式快速打开不同设备或不同服务地址。

## 相对原版的主要优化

### 1. 输入框与键盘适配优化

优化了首次打开软件后点击输入框无反应的问题。现在进入 WebUI 后，点击输入框可以正常唤起手机键盘。

同时针对键盘收起后的界面位置进行了适配，减少输入框被键盘遮挡、键盘收起后页面不回落等问题，让手机端输入体验更接近原生 App。

优化了点击键盘收起按钮后输入框不回落的问题。现在用户直接收起键盘时，输入框和页面位置也会跟随恢复，不需要再额外点击页面空白区域。

### 2. 字体包与项目资源一致

补充并整理了字体包和相关项目资源，尽量保持与原项目 WebUI 的显示效果一致。

这样在手机 App 中开启对应字体显示时，不会因为缺少字体包导致显示异常或风格不一致。

### 3. 首页连接体验优化

重新设计了 App 首页，首次打开时用户可以输入 WebUI 地址进行连接。连接成功后，App 会记住上次使用的地址，后续打开可以直接继续连接。

首页新增了以下入口：

- 扫码连接：通过扫描电脑端二维码快速连接。
- 已保存连接：保存多个 WebUI 地址，方便在不同电脑、不同服务器、不同服务地址之间切换。
- 继续上次连接：快速打开上一次成功连接的 WebUI。
- 手动输入地址：适合首次连接或临时输入新的链接。

### 4. 已保存连接功能

新增“已保存连接”功能。每次成功连接过的地址会自动保存，不会重复保存相同链接。

用户可以在已保存连接里：

- 查看历史连接地址。
- 点击某个连接快速打开。
- 给连接设置备注名称，方便区分不同设备或不同 WebUI。
- 删除不再需要的连接记录。

这个功能适合有多个 Codex Mini WebUI 地址的用户，比如家里电脑、公司电脑、服务器、公网地址、局域网地址等。

### 5. 悬浮窗功能

新增侧边悬浮窗，避免把功能按钮固定占用在页面顶部，让 WebUI 页面可以最大化显示。

悬浮窗支持拖动、侧边吸附、透明度和大小设置。点击悬浮窗后，会打开一个功能面板。

悬浮窗面板内包含：

- 下载管理：打开 App 内置下载界面。
- 回到连接页：从 WebUI 返回连接首页，方便重新输入或切换链接。
- 刷新当前页面：刷新当前 WebUI。
- 悬浮窗设置：调整悬浮窗大小、透明度、主题模式。
- 通知设置：切换任务通知模式。

悬浮窗主题支持深色、浅色、跟随系统，也可以开启液态玻璃样式。下载界面会跟随相同的主题与液态玻璃设置。

### 6. 回到连接页功能

针对原来连接进入 WebUI 后不方便返回连接页的问题，新增了“回到连接页”入口。

用户可以通过悬浮窗中的“回到连接页”返回 App 连接页，然后重新扫码、输入地址、选择已保存连接或继续上次连接。

### 7. 下载管理界面

针对原来点击下载后不知道文件下载到哪里、无法在 App 内查看下载进度和文件的问题，新增了下载管理界面。

下载界面支持：

- 查看下载记录。
- 查看下载状态和下载时间。
- 打开已下载文件。
- 分享已下载文件。
- 删除下载文件。
- 批量选择和批量删除。
- 长文件名自动换行和省略显示。
- 下载记录按时间排序，最新下载显示在最上方。

删除下载记录时，会同时删除手机本地实际下载文件，不只是从列表里隐藏。

下载记录会像浏览器下载记录一样保存。正常覆盖安装或升级 App 后，下载界面仍会保留之前的下载记录。

### 8. 通知功能优化

优化了任务通知方式。原来更偏向常驻通知，现在新增了可选择的通知模式。

通知模式包括：

- 任务结束通知：只有任务完成、失败或异常时才发送通知。
- 常驻通知：App 连接后保持任务通知状态，并在任务运行、完成、失败或异常时更新通知。

这样用户可以根据自己的使用习惯选择通知方式。如果不希望通知栏长期显示，可以选择“任务结束通知”；如果希望随时看到任务运行状态，可以选择“常驻通知”。

无论选择哪种模式，当任务完成、失败或异常且用户不在 App 界面时，系统都会在屏幕顶部显示类似聊天消息的横幅提醒。如果用户正在 App 界面内，则不会弹出顶部横幅。

### 9. 多任务通知优化

当多个会话任务同时运行时，每个会话会有独立通知。对应会话的任务状态变化时，会更新该会话自己的通知，而不是全部挤在同一个通知里。

这样可以更清楚地区分不同会话的运行、完成或异常状态。

选择常驻通知模式后，通知会立即显示。没有任务时显示“空闲中”，有任务时显示运行任务数量。常驻模式会使用 Android 前台服务，降低 App 放到后台后任务监听被系统暂停的概率。

### 10. App 内网页浏览

WebUI 中的网页链接可以直接在 App 内打开，不需要跳出到其他浏览器。

打开的网页会显示在原 WebUI 上方。用户可以通过悬浮窗关闭当前网页并返回原来的会话页面，也可以在手机模式和桌面模式之间切换。

关闭网页后，对应网页 WebView 会被销毁，不会持续留在后台占用资源。

### 11. 局域网线路识别优化

针对 WebUI 在局域网和外网之间切换不够灵敏的问题，App 增加了辅助检测逻辑。

当手机和电脑处于同一局域网时，App 会尽量帮助 WebUI 使用本地线路，同时保持浏览器界面的公网 URL 不变。这样既不改变用户看到的地址，又能在支持的情况下提升局域网访问体验。

## 使用帮助

### 首次连接

1. 打开 AI Mini。
2. 在首页输入 WebUI 地址，或点击“扫码连接”扫描电脑端二维码。
3. 点击连接后进入 WebUI。
4. 连接成功后，该地址会自动保存，后续可以通过“继续上次连接”或“已保存连接”快速打开。

### 切换不同设备或链接

如果你有多个 WebUI 地址，可以使用“已保存连接”。

进入首页后点击“已保存连接”，选择需要打开的地址即可快速切换。也可以给不同地址设置备注名称，例如“家里电脑”“服务器”“公司电脑”等。

### 回到连接页

进入 WebUI 后，如果需要重新输入地址或切换设备：

1. 点击屏幕侧边的悬浮窗按钮。
2. 在弹出的功能面板里点击“回到连接页”。
3. 回到连接页后重新选择连接方式。

### 查看下载文件

当 WebUI 里触发文件下载后，可以通过悬浮窗里的“下载管理”打开下载界面。

在下载管理界面中可以：

- 点击“打开”查看文件。
- 点击分享图标把文件发送给其他 App。
- 点击删除图标删除本地文件。
- 进入管理模式后批量删除多个下载文件。

### 设置悬浮窗

点击悬浮窗后进入功能面板，再点击“悬浮窗设置”。

可以调整：

- 悬浮窗大小。
- 悬浮窗透明度。
- 悬浮窗主题：深色、浅色、跟随系统。
- 液态玻璃：同时应用到悬浮按钮、悬浮菜单、设置面板和下载管理界面。

设置会自动保存，下次打开 App 会继续使用上次的设置。

### 设置通知模式

点击悬浮窗后进入功能面板，再点击“通知设置”。

可以选择：

- 任务结束通知：适合只想在任务完成或失败时收到提醒的用户。
- 常驻通知：适合希望随时看到任务运行状态的用户。

## 注意事项

- 如果之前安装过 debug 测试包，安装正式 release 包时可能需要先卸载旧版本。
- 正常覆盖安装或升级 release 包时，App 设置、已保存连接和下载记录会保留。
- 如果手动卸载 App 后重新安装，Android 可能会清除 App 本地配置，已保存连接和下载记录可能无法保留。
- 下载文件通常保存在手机系统下载目录中。下载界面删除文件时，会真实删除本地文件。
- 如果某些文件在外部文件管理器里被手动删除，App 下载列表中的记录可能仍会显示，但打开或分享时会失败，需要手动删除该记录。

## 版本说明

### v1.9

- 在 Android 15 及以上系统恢复原生状态栏安全区域，避免 WebUI 顶部按钮与时间、电量、网络等系统图标重叠。
- 修复 GeckoView 文件选择器对部分相册、文件管理器返回的 `content://` 文件无法正确上传的问题。
- 文件上传前会在 App 缓存中生成 GeckoView 可读取的临时副本，支持单文件和多文件上传。
- 重做 GeckoView 下载处理：优先直接保存 Gecko 返回的响应数据，不再对下载地址进行容易丢失登录状态的二次请求。
- blob、data 和 WebUI 附件下载改为分块传输，避免较大文件超过 WebExtension 消息大小限制后下载失败。

### v1.6

- 浏览器内核从系统 Android WebView 迁移到 Mozilla GeckoView。
- WebUI 原版液态玻璃由 Gecko 原生渲染，不再进行滚动时降级或动态切换。
- 主 WebUI、App 内临时网页、手机/桌面模式、前进后退、文件选择和下载均迁移到 GeckoView。
- 悬浮窗设置新增“液态玻璃”，并同步应用到悬浮按钮、悬浮菜单、设置面板与下载管理界面。
- 深色、浅色、跟随系统改为更紧凑的分段布局。
- 悬浮窗“下载”改名为“下载管理”。
- 常驻通知改用前台服务，选择后立即显示；无任务时显示“空闲中”。
- 优化 App 后台任务完成、失败或异常通知的稳定性。
- APK 改为 `arm64-v8a` 与 `armeabi-v7a` 分包。
- 最低支持系统调整为 Android 8.0。

### v1.5

- 移除 v1.4 的液态玻璃动态分级和滚动状态切换。
- 无论静止、慢速滑动还是快速滑动，始终保持 WebUI 原版液态玻璃外观。
- 不再动态改变模糊半径、折射透明度、背景颜色或阴影。
- 针对 Android WebView 将玻璃组件提升到稳定的独立硬件合成层。
- 优化长会话中屏幕外消息的渲染，降低滑动时玻璃背景重复采样的开销。
- 自动清理玻璃组件中完全重复的叠加层，不删除原版模糊、折射、高光或阴影层。
- 新增 DOM 监听，新出现的设置弹窗、菜单和输入框玻璃组件也会自动应用相同优化。

### v1.4

- 液态玻璃优化由固定轻量模式升级为动态分级模式。
- 页面静止时保留 WebUI 原版液态玻璃模糊、折射和阴影效果。
- 轻微滑动时仅小幅降低模糊强度，不突然关闭玻璃效果。
- 中速滑动时暂停高开销的 SVG 折射，同时保留轻量实时背景模糊。
- 快速滑动或检测到明显掉帧时，临时切换到同色调玻璃缓存效果。
- 停止滑动后按“快速→中等→轻微→原版”的顺序平滑恢复，减少视觉割裂。
- 动态分级同时参考滑动速度和实际渲染帧耗时，可根据不同安卓手机性能自动调整。

### v1.3

- 针对 Android WebView 优化液态玻璃模式，移除高开销的实时背景模糊和 SVG 位移滤镜，改用轻量化玻璃渐变与阴影效果。
- 降低液态玻璃模式下滑动消息列表、打开设置和输入区域时的掉帧。
- 重做桌面模式：同时切换桌面浏览器 UA 和 980px 桌面布局视口。
- 桌面模式支持双指缩放，最大可放大至 5 倍。
- 桌面模式现在同时支持主 WebUI 和 App 内打开的临时网页。
- 手机模式与桌面模式切换后会重新载入页面，确保响应式布局正确更新。

### v1.2

- 修复 App 切换到后台后，任务完成或失败时可能收不到状态栏通知和顶部横幅通知的问题。
- App 在后台且仍保留在系统进程中时，由原生层主动维持任务状态检查。
- App 前台显示时不弹顶部横幅；切换到后台后，任务完成或失败会显示顶部横幅。
- “任务结束通知”模式仍只在任务结束时显示状态栏通知。
- “常驻通知”模式继续在状态栏显示连接、运行和结束状态。

### v1.1

- App 显示名称统一改为 AI Mini，扫码界面同步更新。
- 新增任务完成、失败/异常时的顶部横幅通知；App 位于前台时不弹出横幅。
- 支持在 App 内打开 WebUI 中的网页链接。
- 外部网页作为临时页面显示，可通过悬浮窗关闭并销毁，不占用后台资源。
- 临时网页支持手机模式与桌面模式切换。

### v1.0

- 完成 Android App 基础 WebView 访问能力。
- 优化手机键盘输入体验。
- 新增首页连接页。
- 新增扫码连接、继续上次连接、已保存连接。
- 新增侧边悬浮窗和功能面板。
- 新增下载管理界面，支持打开、分享、删除、批量管理。
- 新增下载记录持久化。
- 新增任务完成、失败/异常通知。
- 新增常驻通知模式。
- 优化多会话任务通知显示。
- 优化字体包和资源一致性。
- 优化局域网线路识别体验。
