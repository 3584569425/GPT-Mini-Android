# 外部 v1.19 修复包合并说明

## 来源

- 导入包：`GPTMini-v1.19.zip`
- SHA-256：`7aeb7f9b02e484375a3afae1935bd1ce8b2c0f1bbc7ee34b52b80ce81b2cc915`
- `GPTMini-v1.19_副本.zip` 与上述文件逐字节相同，仅为重复副本。
- 外部包内嵌 Git 仓库包含以下提交：
  - `f1f4e61`：建立 GPT Mini Android v1.19 接手基线；
  - `11f1230`：修复多设备切换后输入框与液态玻璃失效；
  - `4f14abb`：将应用版本改为 `V1.2.0`。

## 已导入内容

本次从外部基线中筛选并导入：

- 后台通过 `/codex/threads` 发现运行任务；
- 利用线程运行快照恢复轮询间隔内完成的短任务通知；
- 任务完成和失败通知显示线程名称、回复摘要与任务用时；
- 通知增加“查看回复”和“标为已读”操作；
- 点击完成通知后打开对应线程；
- WebExtension 消费 `codex:taskstate` 稳定事件；
- 补充线程名称和任务摘要获取；
- Android 15 状态栏背景兼容；
- 多设备切换时忽略 URL 查询参数差异，保持使用主 GeckoView，避免输入框和液态玻璃补丁失效。

## 未导入内容

- 外部提交 `4f14abb` 中将版本从 `1.19` 改成 `V1.2.0` 的修改，避免打乱当前项目版本线；
- `.tools/` 中附带的 Gradle、Android SDK 等本机工具；
- `.gradle/`、`build/`、`app/build/` 构建缓存；
- APK、调试截图、日志、临时文件；
- `__MACOSX/` 和 `.DS_Store`；
- 外部包中针对另一台电脑路径编写的本机说明。

外部包没有新增 Gradle 依赖声明。项目仍然只保留 ZXing 和 GeckoView 两项应用依赖。

## Git 回退点

合并前的原始版本通过以下引用保留：

- 分支：`codex/pre-external-v1.19-merge`
- 标签：`pre-external-v1.19-merge-20260715`

需要完全恢复合并前版本时，可从该分支或标签创建新分支，不需要删除当前合并历史。
