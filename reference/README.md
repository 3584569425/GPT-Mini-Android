# 参考源码（只读）

本目录存放 **GeckoView v1.25.1** 的源码副本，仅供旧版 WebView 工程对照移植功能时使用。

## 约定

- 路径：`reference/GeckoView-v1.25.1/`
- **只读参考**：不要在本目录里改功能或当正式工程构建
- 正式开发与构建仍在旧版根目录自身的 `app/` 等进行
- 不会影响外部工程 `../GPTMini-GeckoView-v1.25.1`

## 复制时已排除

- `.git`、`.tools`、`deliverables`
- `build` / `.gradle` / `*.apk` / `*.aab`
- `local.properties`、密钥文件（`keystore/*.jks` 等）

源码版本快照对应：`GPTMini-GeckoView-v1.25.1`（versionName 1.25.1）。
